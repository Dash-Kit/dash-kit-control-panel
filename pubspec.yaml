name: flutter_platform_control_panel
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  alice:
    git:
      url: git://github.com/MrtnvM/alice.git

  shared_preferences: ">=0.5.3 <1.0.0"
  package_info: 0.4.0+6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
