name: example
description: Example project for Dash-Kit Control Panel

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dash_kit_control_panel:
    path: ../
  dio: ^5.4.2+1
  alice_lightweight: ^3.6.0
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/png/